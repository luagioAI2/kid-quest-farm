/* ============================================================
   头像候选
   ------------------------------------------------------------
   抽出来是因为**两个地方都要用**：设置页的「选一个头像」，
   和新手引导第一步。以前只有设置页有，引导再抄一份的话，
   以后加一个头像就会只加一边 —— 孩子在引导里选不到、在设置里能选到。

   只放 emoji，不放图片（本项目「图片零落盘」，见 README）。
   ============================================================ */

export const AVATAR_CHOICES = [
  '🐻',
  '🐰',
  '🐱',
  '🐶',
  '🦊',
  '🐼',
  '🐨',
  '🦁',
  '🐯',
  '🐸',
  '🐵',
  '🦄',
] as const

/** 默认头像。和 `DEFAULT_SETTINGS.avatar` 必须一致。 */
export const DEFAULT_AVATAR = '🐻'
